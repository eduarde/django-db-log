============
Installation
============

At the command line::

    $ easy_install django_db_log

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_db_log
    $ pip install django_db_log
